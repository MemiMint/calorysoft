import { FunctionComponent } from "react";
import { RegistryWizard } from "../../components/RegistryWizard";

export const Registry: FunctionComponent = (): JSX.Element => {
    return (
        <RegistryWizard />
    )
}